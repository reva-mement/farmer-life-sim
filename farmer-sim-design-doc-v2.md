# 箱庭農民シミュレーター 開発環境移行ドキュメント (v2)

前回の `farmer-sim-design-doc.md` の後継。Three.js版の試作を通じて分かったことを
すべて反映している。Claude Codeでのプロジェクト開始時に、このファイルをまず
読み込ませること。

## 1. 開発環境について(なぜ移行するか)

claude.aiのアーティファクト環境には、今回の開発で以下の構造的な限界が判明した。

- **Claudeが実際の描画結果を見られない**: WebGLの画面をClaude自身が確認する手段がなく、
  ユーザーがスクリーンショットを貼る→Claudeが推測で直す、のサイクルでしか検証できない。
  今回、原因特定に5往復以上かかったバグ(白い線の件、後述)があった。
- **1ファイルにすべて詰め込む制約**: プロジェクトが育つにつれ、ファイル分割ができない
  ことがボトルネックになった。
- **バージョン管理がない**: gitのような差分管理・ロールバックができない。
- **アセットパイプラインがない**: Blender等で作った実モデルを読み込めない。
- **周辺ライブラリが使えない**: OrbitControls、postprocessing(ブルーム等)、GLTFLoaderが
  使用不可(three.js r128の素の機能のみ)。

**Claude Codeでの改善点**: Playwright等でブラウザを自動操作しスクリーンショットを撮る
仕組みを組み込めば、Claude自身が画面を見ながら直接デバッグできる。npm経由で周辺
ライブラリもフル活用でき、git管理・ファイル分割も可能になる。

### 最初にやるべきこと(セットアップ)
1. Vite + React + Three.js(最新版)のプロジェクト雛形を作成
2. **Playwrightによる自動スクリーンショット検証ループ**を最優先で構築する。
   具体的には、開発サーバーを起動→Playwrightでページを開く→canvas要素を
   スクリーンショット→Claudeがそれを`view`で確認、という一連の流れをスクリプト化する。
   これが今回の開発で最も苦労した部分(見えないままデバッグする)を解消する。
3. `git init`して、意味のある単位でコミットしていく
4. 現在のGRID_SIZE=11、1ユニット=1タイルという座標系を踏襲する(下記参照)

## 2. 踏んだ地雷(同じバグを繰り返さないための記録)

いずれも実際に発生し、原因特定〜修正まで時間を要したもの。

1. **Three.jsのオイラー回転は「内因性(intrinsic)」**: `mesh.rotation.x`と
   `mesh.rotation.y`を両方設定すると、「まず世界のX軸で回転→次に世界のY軸で回転」には
   ならない。2軸目の回転は「1軸目の回転で変化した後のオブジェクト自身のローカル軸」に
   対してかかる。タイルを「寝かせてからその場でスピンさせる」つもりが、寝かせた後の
   スピンが横倒しの回転になってしまい、タイルが浮いて傾いて見えるバグになった。
   **対策**: 複数軸の回転を1つのメッシュに重ねがけしない。「寝かせる」変換は
   `geometry.rotateX(-Math.PI/2)`でジオメトリ自体に一度だけ焼き込み、メッシュ側は
   単一軸(例: Y軸のみ)の回転に限定する。
2. **BFS経路探索の判定関数を逆に渡すバグ**: `walkable`(通行可能)を期待する引数に
   `isBlocked`(通行不可)をそのまま渡してしまい、ほぼ全てのクリックで
   「経路なし」判定になっていた。関数を渡す際は意味が逆になっていないか要確認。
3. **シャドウアクネ(影のちらつき)**: 関節で動くキャラクターや、細い葉が大量にある
   オブジェクト(稲)に`castShadow=true`を設定すると、継ぎ目や根元でちらつく。
   **対策**: 小さく数の多いオブジェクトは`castShadow=false`にして、代わりに
   足元に固定の円形デカール(疑似AO)を敷いて「地面に乗っている」感を出す。
4. **AOデカールと凹凸地形のZファイティング**: 地面に実際の凹凸(頂点変位)を
   付けると、固定高さの影デカールが地形の出っ張りに埋もれたり浮いたりして
   ちらつく。**対策**: デカールのマテリアルで`depthTest: false`を設定し、
   奥行き判定を無視して常に地面の上に描画されるようにする。
5. **テクスチャの継ぎ目**: fbmノイズで生成したテクスチャは、境界がシームレスに
   繋がる保証がない。`repeat.set(2,2)`のように繰り返し設定すると、繰り返しの境目
   (ちょうど半分の位置など)で模様が食い違い、目立つ線として見える。
   **対策**: 繰り返しが必要なければ`repeat.set(1,1)`(繰り返しなし)にする。
   繰り返したい場合はノイズ関数自体をシームレスにする必要がある。
6. **自前でBufferGeometryを組むときはUV座標を忘れずに**: 位置(position)属性だけ
   設定してテクスチャ付きマテリアルを使うと、UVが無い状態でのテクスチャ描画は
   ブラウザ・GPU依存の未定義に近い挙動になり、カメラを動かすと一緒に動く
   「白い線」のような不可解な描画崩れにつながることがあった。標準ジオメトリ
   (BoxGeometry等)はUVを自動計算してくれるので、可能な限りそちらを使う。
7. **MeshStandardMaterial + 強い directional light + ACESFilmicToneMapping で
   色が白飛びすることがある**: 特に垂直な面(壁など)で顕著だった。色の再現性を
   最優先したい場合は`MeshStandardMaterial`をやめて`MeshBasicMaterial`
   (光源の影響を受けないベタ塗り)にすると確実。质感(陰影)より色の正確さを
   優先する場面で有効。
8. **Shapeに穴(hole)を開けるとブリッジの継ぎ目が出ることがある**: 地面に
   穴を開けるのに`THREE.Shape`+`holes`+`ShapeGeometry`を使うと、外周と穴を
   つなぐ三角形(ブリッジ)が生成され、テクスチャの継ぎ目として見えることが
   あった。**対策**: 穴を開けたい場合は、素直に「複数の重ならない板を並べて
   囲む」方式にする(面積が過不足なく一致するよう座標を計算する)。

## 3. 確立した技術方式

### 地形(土・道)
- 実際に頂点を変位させる「本物の凹凸」+ Canvas生成の色/凹凸(bump)/艶(roughness)
  マップの組み合わせ。1タイル分の解像度は8分割程度が性能とのバランスが良い。
- 複数種類の地面(土・道など)を1枚の連続した地形として滑らかにブレンドする
  手法も検証済み(`smoothstep`で2種類のノイズサンプルを混ぜる)。継ぎ目のない
  地形にする場合はこちらを使う。
- 色のブレンド元は `village-path-walk-study.jsx` の `SOIL` / `PATH` オブジェクトを
  正とする。

### キャラクター(農民)
- **骨格(関節)方式**: 「◯と■を組み合わせた人形」ではなく、関節点(hip, knee,
  shoulder, elbow等)を`THREE.Group`の親子構造で定義し、各関節グループの
  **単一軸(X軸)回転だけ**で手足を動かす。`boneLocal(length, radiusA, radiusB, ...)`
  関数が、関節のローカル原点(0,0,0)から真下(0,-length,0)に伸びる「服を着た骨」を
  作る。歩行アニメーションはこの関節を位相(phase)に応じて回転させるだけで実現できる。
- 胴体だけは輪郭線回転成形(LatheGeometry)で作り、着物らしい裾の広がりを出す。
- 史実確認済み: 農民の服は麻・木綿主体で藍染めや生成りが典型色、草鞋(わらじ)+
  脚絆(すねの布巻き)、菅笠(すげがさ)が一般的。今の実装はこれに準拠している。
- 歩行時は「脚が交差する瞬間に速く、踏み出しきった瞬間に緩む」ように前進速度を
  位相と連動させると自然に見える。歩幅(脚の振り角)と速度は独立したパラメータ。
- 端に着いたら停止→脚を中立姿勢に戻しつつ180度振り返る、というターン処理も実装済み。

### 田んぼ
- 畦の高さは実測で標準30cm、水深は5〜6cmが目安(農水省等の資料で確認済み)。
- 水面は色だけでなく、fbmノイズによるbumpMapも持たせ、色・凹凸マップの両方を
  毎フレームオフセットすることで波が動いているように見せる。
- 稲は葉6〜9枚をランダムな向きでまとめた「株」を等間隔グリッドで配置。
  8月上旬は穂が出る前の青々とした状態が史実的に正しい。
- **稲の色は`MeshBasicMaterial`のベタ塗りで統一**(陰影によるムラをユーザーが
  望まなかったため)。

## 4. 引き継ぐ参考実装ファイル

このチャットのアーティファクトとして以下を作成済み。Claude Codeのプロジェクトに
コピーして、リファレンス実装として参照すること(そのまま使うのではなく、
上記の教訓を踏まえてリファクタリングしながら本実装に組み込んでいく想定)。

- `farmer-life-sim.jsx` — 最初期のSVG版プロトタイプ(参考程度)
- `farmer-life-sim-3d.jsx` — Three.js版の村全体(地形・建物・NPC・農民・移動)
- `soil-tile-study.jsx` / `path-tile-study.jsx` — 土/道タイル単体の検証
- `soil-path-transition-study.jsx` — 土→道のシームレスなブレンド検証
- `soil-grid-study.jsx` / `village-ground-path-study.jsx` — 地形を1枚の連続
  メッシュとして構築する方式の検証
- `farmer-character-study.jsx` — 農民キャラクター単体(骨格方式)の検証
- `village-path-walk-study.jsx` — 地形+農民の歩行アニメーション統合版
  (**現時点で最も完成度が高い参考実装**)
- `rice-paddy-study.jsx` — 田んぼ単体(畦・水・稲)の検証

## 5. 世界のスケール

農民の身長(約0.94ユニット)を実寸1.5〜1.6m相当として、おおよそ
**1ユニット ≈ 1.6〜1.7m**、または稲の背丈(0.15ユニット=実寸50cm)を基準にすると
**1ユニット ≈ 3.3m**で計算している箇所が混在している。Claude Codeでの本実装では、
どちらかに統一するか、農民と田んぼで別スケールを使う場合はその境界を明確に
ドキュメント化すること。

## 6. 次のマイルストーン案
1. Vite+React+Three.js雛形の作成、Playwright検証ループの構築
2. 上記の参考実装をリファクタリングしながら1つのコードベースに統合
3. NPC・会話・好感度システムの移植(元々のSVG版で実装済み、3D版には未移植)
4. 田んぼ・農作業(耕す→田植え→収穫)システムの本実装
5. 季節・天候などの拡張要素(未着手)
